<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Campo para el Nombre de la Convocatoria -->
    <EditText
        android:id="@+id/convocatoriaNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre de la convocatoria"
        android:layout_marginBottom="16dp" />

    <!-- Botón para Seleccionar Fecha de Inicio -->
    <Button
        android:id="@+id/startDateButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Seleccionar fecha de inicio"
        android:layout_marginBottom="8dp" />

    <!-- Botón para Seleccionar Fecha de Fin -->
    <Button
        android:id="@+id/endDateButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Seleccionar fecha de fin"
        android:layout_marginBottom="16dp" />

    <!-- Botón para Guardar Convocatoria -->
    <Button
        android:id="@+id/saveConvocatoriaButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Guardar Convocatoria"
        android:layout_marginBottom="24dp" />

    <!-- RecyclerView para Mostrar Convocatorias -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/convocatoriasRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="8dp" />
</LinearLayout>
